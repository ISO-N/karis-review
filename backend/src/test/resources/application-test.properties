# 测试环境配置：系统测试会频繁调用注册/发码接口，关闭限流避免 429 干扰
app.rate-limit.enabled=false

# 测试环境使用本地磁盘备份存储（避免依赖 MinIO）
backup.storage.type=local
backup.storage.local-dir=${java.io.tmpdir}/karis-review-test-backups

# 测试环境不投递外部事件
app.events.external-endpoint=
